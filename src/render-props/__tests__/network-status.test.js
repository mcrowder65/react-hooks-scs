import React from "react";

import { render } from "../../../test/utils";
import NetworkStatusRenderProps from "../network-status";

test("it renders", () => {
  render(<NetworkStatusRenderProps />);
});
