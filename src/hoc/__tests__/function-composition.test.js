import React from "react";

import { render } from "../../../test/utils";
import FunctionComposition from "../function-composition";

test("it renders", () => {
  render(<FunctionComposition />);
});
